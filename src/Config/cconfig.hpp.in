//
// Created by pi on 26/11/2019.
//

#ifndef BEHOLDER_CONFIG_HPP_IN
#define BEHOLDER_CONFIG_HPP_IN

#include <cstdint>

namespace Config {
    const uint8_t VERSION_MAJOR = 0;
    const uint8_t VERSION_MINOR = 1;

    const uint32_t SERIAL_RESPONSE_TIME_MILLIS = @SERIAL_RESPONSE_TIME@;
    const char SERIAL_STATE_CHAR = '@SERIAL_STATE_CHAR@';
    const char SERIAL_TEST_CHAR = '@SERIAL_TEST_CHAR@';

    const uint16_t REMOTE_CONTROL_PORT = @REMOTE_CONTROL_PORT@;
    const uint16_t REMOTE_STREAM_PORT = @REMOTE_STREAM_PORT@;

    const float ROBOT_SPEED_PER_SEC = @ROBOT_SPEED_PER_SECOND@;
    const float TRIGGER_FIRING_THRESHOLD = @TRIGGER_FIRING_THRESHOLD@;
}

#endif //BEHOLDER_CONFIG_HPP_IN
